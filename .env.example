# 默认无需配置；按需取消注释并修改。
# 本地开发：复制为 .env.local；托管部署：在平台中设置环境变量。
# Docker：通过 -e 或 --env-file 传入容器。
# Docker Compose：新增变量需写入 docker-compose.yml 的 environment，
# 或通过 env_file 加载；仅放在宿主机 .env 中不会自动传入容器。

# 服务端代理的后端地址，默认 https://api.duckmail.sbs。
# Docker 可在运行时设置；仅更换代理后端时设置此项即可。
# API_BASE_URL=https://api.duckmail.sbs

# 构建时写入浏览器的默认提供商地址，默认 https://api.duckmail.sbs。
# 服务端未设置 API_BASE_URL 时也使用此值。修改后需重新构建。
# 此变量公开可见，不得放入密钥。
# NEXT_PUBLIC_API_BASE_URL=https://api.duckmail.sbs

# 额外允许代理访问的 API 来源，逗号分隔，格式为 https://主机[:端口]，不含路径。
# 自定义提供商需在此放行；配置的后端、DuckMail 和 Mail.tm 已默认允许。
# DUCKMAIL_ALLOWED_API_ORIGINS=https://mail-api.example.com

# 可选：至少 32 字节，与后端 INBUCKET_DUCKAPI_WEB_PROXY_SECRET 一致。
# 豁免 Web 代理请求的普通限流与异常 IP 降级，不免除账户鉴权。
# 仅放在服务端，不得使用 NEXT_PUBLIC_ 前缀；公开代理仍可被脚本模拟访问。
# DUCKMAIL_WEB_PROXY_SECRET=

# 默认 false。仅在 Web 端口不对公网开放、入口代理覆盖 X-Real-IP，
# 且后端已配置可信代理时启用，以转发真实客户端 IP。
# 否则后端可能按 Web 服务器出口 IP 共用限额。
# DUCKMAIL_TRUST_PROXY_HEADERS=false
